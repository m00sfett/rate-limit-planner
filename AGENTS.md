# AGENTS.md

- Keep code, comments, UI text, and documentation in English.
- Make granular, reviewable commits; avoid bundling unrelated changes.
- Treat `.settings.json` as local state and do not commit it.
- When changing the UI, verify that the window still opens cleanly and that the output area is fully visible.
